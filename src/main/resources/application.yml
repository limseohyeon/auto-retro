#테스
spring:
  application:
    name: auto-retro

  modulith:
    detection-strategy: explicitly-annotated

  batch:
    job:
      enabled: false

  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
